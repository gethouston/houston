{
  "name": "houston-ai",
  "version": "0.4.12",
  "private": true,
  "type": "module",
  "workspaces": [
    "ui/*",
    "app",
    "mobile",
    "houston-relay",
    "examples/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "typescript": "^6.0.2"
  },
  "engines": {
    "bun": ">=1.3.10"
  }
}

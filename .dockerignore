# Root-context Docker builds (selfhost/Dockerfile, packages/runtime/Dockerfile,
# packages/control-plane/Dockerfile) need sibling packages on disk for `file:`
# deps. Keep dev artifacts out of the context while preserving source trees.
**/node_modules
.git
**/target
**/*.log
**/.DS_Store
.context

/**
 * The per-agent Files wiring, mounted by the employee screen's Files section.
 * `useAgentFiles` is the single assembler behind it.
 */
export type { AgentFiles } from "./use-agent-files";
export { useAgentFiles } from "./use-agent-files";

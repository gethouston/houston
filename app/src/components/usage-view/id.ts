/**
 * The `viewMode` value for the top-level Usage page.
 *
 * Like the other top-level view ids (`"dashboard"` / `"settings"` /
 * `INTEGRATIONS_VIEW_ID`), it must live OUTSIDE `STANDARD_TAB_IDS` so it can
 * never shadow a per-agent tab.
 */
export const USAGE_VIEW_ID = "usage";

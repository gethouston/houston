/**
 * The phone's Teams tab root: every team as a tree row with its sections
 * indented beneath it. Placeholder until the real screen lands.
 */
export function TeamsHomeView() {
  return <div data-testid="teams-home" />;
}

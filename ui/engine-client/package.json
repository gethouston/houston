{
  "name": "@houston-ai/engine-client",
  "version": "0.4.0",
  "description": "The deployment-agnostic half of the Houston engine client surface: protocol v3 wire types, anonymous Agent Store catalog reads, the local-model-bridge port, and the Retry-After parser. The client itself is the engine adapter in packages/web/src/engine-adapter, which this specifier resolves to.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "test:assistant-catalog": "vitest run tests/*.spec.ts",
    "typecheck": "tsgo --noEmit && tsgo -p scripts/tsconfig.json --noEmit"
  },
  "dependencies": {
    "@houston/agentstore-client": "workspace:*",
    "@houston/protocol": "workspace:*"
  },
  "devDependencies": {
    "@houston/domain": "workspace:*",
    "typescript": "5.9.3",
    "vitest": "3.2.6"
  }
}
